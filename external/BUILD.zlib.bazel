CC_OPTS = select({
    "@bazel_tools//src/conditions:windows": ["/std:c++17"],
    "//conditions:default": ["--std=c++17"],
})

cc_library(
    name = "zlib",
    hdrs = glob(["*.h"]),
    srcs = glob(["*.c"]),
    copts = CC_OPTS,
    includes = ["."],
    visibility = ["//visibility:public"],
)
